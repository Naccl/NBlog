<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.naccl.mapper.VisitLogMapper">

    <resultMap id="log" type="top.naccl.entity.VisitLog">
        <id property="id" column="id"/>
        <result property="uuid" column="uuid"/>
        <result property="uri" column="uri"/>
        <result property="method" column="method"/>
        <result property="param" column="param"/>
        <result property="behavior" column="behavior"/>
        <result property="content" column="content"/>
        <result property="remark" column="remark"/>
        <result property="ip" column="ip"/>
        <result property="ipSource" column="ip_source"/>
        <result property="os" column="os"/>
        <result property="browser" column="browser"/>
        <result property="times" column="times"/>
        <result property="createTime" column="create_time"/>
        <result property="userAgent" column="user_agent"/>
    </resultMap>

    <!--查询日志-->
    <select id="getVisitLogList" resultMap="log">
        select id, uuid, uri, method, param, behavior, content, remark, ip, ip_source, os, browser, times, create_time from visit_log
    </select>

    <!--添加日志-->
    <insert id="saveVisitLog" parameterType="top.naccl.entity.VisitLog" useGeneratedKeys="true" keyProperty="id">
        insert into visit_log (uuid, uri, method, param, behavior, content, remark, ip, ip_source, os, browser, times, create_time, user_agent)
        values (#{uuid}, #{uri}, #{method}, #{param}, #{behavior}, #{content}, #{remark}, #{ip}, #{ipSource}, #{os}, #{browser}, #{times}, #{createTime}, #{userAgent})
    </insert>

    <!--删除日志-->
    <delete id="deleteVisitLogById">
        delete from visit_log where id=#{id}
    </delete>
</mapper>